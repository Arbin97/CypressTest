describe('My First Test', () => {
    it('Does not do much!', () => {
      expect(true).to.equal(true)
    })

    // Not Failing Test case
    it('summation', () => {
        expect(2+3).to.equal(5)
      })
  })